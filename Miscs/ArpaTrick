Basically instead of doing : map<int,map<int,int>> and do some sort of recursion
create only one map for the root and create the rest of the maps during the recursion and do the rest of the operation on them
it optimizes the memory.
